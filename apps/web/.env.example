# Local development environment template for apps/web.
# `./dev-setup.sh up` will copy this file to `apps/web/.env` if it is missing.

# Backend API URL
VITE_API_URL=http://localhost:8031

# Scaffold auth mode for the Phase 1 shell
VITE_SCAFFOLD_AUTH_MODE=anonymous
