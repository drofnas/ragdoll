# `packages/contracts`

Shared contract home for OpenAPI descriptions, schemas, and generated TypeScript types.

Phase 0 creates the package boundary only. Contract generation and concrete schema work are deferred until later phases.

Expected sub-areas:

- `openapi/`
- `schemas/`
- `typescript/`
