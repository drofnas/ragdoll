# `chat`

Source of truth: `apps/api/src/ragdoll/modules/chat/api/schemas.py`

Owns chat message, session, citation packaging, and suggestion contracts.
Sessions may optionally be scoped to one source document while remaining inside a single Space.
