# `packages/contracts/schemas`

Ownership notes for public schema groups.

These directories describe which backend module owns each public contract area. The canonical schema definitions live in backend Python modules, not in this package.
