# `packages/tooling`

Shared repository tooling home.

Phase 0 reserves this package for code generation helpers, structure validation, and small repo-owned support scripts that are not product runtime code.
