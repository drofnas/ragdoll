# `packages/tooling`

Shared repository tooling home.

Phase 2 now reserves this package for code generation helpers, structure validation, and small repo-owned support scripts that are not product runtime code.

Current contract-generation entrypoint:

- `packages/tooling/scripts/generate_contracts.py`
