# `packages/tooling/scripts`

Home for repo-owned helper scripts that support code generation or structure validation without becoming ad hoc product utilities.

Phase 2 currently adds:

- `generate_contracts.py` for OpenAPI export plus TypeScript-generation scaffolding
- `infra_smoke.py` for the opt-in Phase 7 live dependency and ingestion smoke verification
