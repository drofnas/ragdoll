# `packages/config`

Shared configuration and convention home for the repository.

Phase 0 includes:

- `env/` for canonical environment examples
- `lint/` for shared lint and formatting placeholders
- `test/` for shared test config placeholders

Tool-specific configuration can be added here in later phases as long as ownership stays explicit and repo-wide.
