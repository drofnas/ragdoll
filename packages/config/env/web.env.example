# Canonical frontend environment example for apps/web during the rebuild.
# Copy to apps/web/.env for local runtime use.

# Backend API URL
VITE_API_URL=http://localhost:8031

# Scaffold auth mode for the Phase 1 shell
VITE_SCAFFOLD_AUTH_MODE=anonymous
