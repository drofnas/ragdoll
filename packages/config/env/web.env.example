# Mirrored shared frontend environment example for apps/web during the rebuild.
# The primary local template lives at apps/web/.env.example.

# Backend API URL
VITE_API_URL=http://localhost:8031

# Scaffold auth mode for the Phase 1 shell
VITE_SCAFFOLD_AUTH_MODE=anonymous
